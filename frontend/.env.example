# Leer lassen für Dev ohne Login (API läuft dann als User "dev").
# Für den Login-Flow gegen das lokale Keycloak aus docker-compose:
# VITE_KEYCLOAK_URL=http://localhost:8080
# VITE_KEYCLOAK_REALM=smierx
# VITE_KEYCLOAK_CLIENT=smierx-queue
